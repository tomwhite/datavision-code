# Unknown Levels

Preprocess data

```bash
npm install
node index.js
```

```bash
http-server
```

(Install first with `npm install http-server -g`).

Then go to http://localhost:8080/unknown-levels.html
